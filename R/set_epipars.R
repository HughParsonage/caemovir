

set_epipars <- function(reff = list(distribution = "dirac",
                                    centre = 6),
                        p_symp = 0.2,
                        p_hosp = 0.03,
                        p_icu = 0.015,
                        p_kill = 0.009) {
  mget(ls())
}
